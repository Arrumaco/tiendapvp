<p>Dear Customer, </p>

<p>Your order has been CANCELED.  Please retain this cancellation information for your records. </p> 

============================================================ <br />
Orden Número <%= @order.number %> <br /> 
Order Summary [CANCELED] <br />
============================================================ <br />
<% @order.line_items.each do |item| %>
  <%= item.variant.sku %> <%= item.variant.product.name %> <%= variant_options(item.variant) %> (<%= item.quantity %>) @ <%= number_to_currency item.price %> = <%= number_to_currency(item.price * item.quantity) %> <br />
<% end %>
============================================================  <br />
<p>Subtotal: <%= number_to_currency @order.item_total %> <br />
<% @order.adjustments.each do |adjustment| %>
<%= "#{adjustment.label}: #{number_to_currency adjustment.amount}"%> <br />
<% end %>
Order Total: <%= number_to_currency @order.total %>  <br />
